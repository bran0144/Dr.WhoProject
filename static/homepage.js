"use strict";


// $('#search-season-episode').on('click', (evt) => {
//     const formInputs = {
//         'season': $('#season').val(),
//         'episode_number': $('#episode-number').val()
//     };
//     $.get('/map_search/<season>/<episode_number>', formInputs, (res) => {})
// });
// At id #search-season-episode, on the click event, the inputs from the dropdown boxes will be
// submitted as a get request and sent to the /map_search/<season>/<episode> route


// $('#search-doctor').on('click', (evt) => {
//     const formInputs = {
//         'doctor': $('doctor').val()
//     };
//     $.get('map_search/<doctor>', formInputs, (res) => {})
// });
// At id #search-doctor, on the click event, the input from the dropdown box will be
// submitted as a get request and sent to the /map_search/<doctor> route


// $('#episode-title').on('click', (evt) => {
//     const formInputs = {
//         'title': $('title').val()
//     };
//     $.get('map_search/<title>', formInputs, (res) => {})
// });

// At id #episode-title, on the click event, the input from the text box will be
// submitted as a get request and sent to the /map_search/<title> route
